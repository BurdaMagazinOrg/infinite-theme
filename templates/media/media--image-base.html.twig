{#
/**
 * @file
 * Default theme implementation to present a media entity.
 *
 * Available variables:
 * - name: Name of the media.
 * - content: Media content.
 *
 * @see template_preprocess_media()
 *
 * @ingroup themeable
 */
#}


{% if content %}
    <div class="img-container" itemscope="" itemtype="http://schema.org/imageObject">
        {{ content.field_image }}
    </div>
    <div class="caption">
        {% if content.field_copyright | plain_text or content.field_source | plain_text %}
            <div class="text-copyright">
                {% if content.field_copyright | plain_text %}
                    <span>{{ content.field_copyright }}</span>
                {% endif %}
                {% if content.field_source | plain_text %}
                    <span>{{ content.field_source }}</span>
                {% endif %}
            </div>
        {% endif %}
        {% if content.field_description %}
            <div class="text-description">{{ content.field_description }}</div>
        {% endif %}

        <div class="socials socials-bar">
            <div class="item-social icon-facebook" data-url="{{ absolute_node_url | e('url')}}" data-media-url="{{ share_img_url | e('url')}}" data-description="{{ content.field_description | plain_text | e('url') }}" target="_blank" data-social-type="facebook"></div>
            <div class="item-social icon-pinterest" data-url="{{ absolute_node_url | e('url')}}" data-media-url="{{ share_img_url | e('url')}}" data-description="{{ content.field_description | plain_text | e('url') }}" data-social-type="pinterest"></div>
        </div>
    </div>
{% endif %}
