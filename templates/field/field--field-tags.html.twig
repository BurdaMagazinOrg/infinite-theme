{% if items|length > 0 %}
    <div class="item-block item-block-tags">
        <div class="tags">
            <div class="label">{{ 'More'|t }}:</div>
            <ul>
                {% for item in items %}
                    <li>{{ item.content }}</li>
                {% endfor %}
            </ul>
        </div>
    </div>
{% endif %}
