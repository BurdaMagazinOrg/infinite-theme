{% if content.logo %}
<div{{ attributes.addClass('native_campaign_ecommerce_slider') }}>
    <span class="separator">Sponsored by</span>
      <span class="brand">
        {{ content.logo }}
      </span>
</div>
{% endif %}
{{ content.tracking_pixel }}